/**
 * OBSOLETO: el API original (bible-api.deno.dev) está roto y devuelve
 * respuestas vacías, así que ya no se puede sembrar desde ahí.
 * Usa en su lugar:  deno run -A scripts/seed.ts
 */
import "./seed.ts";
